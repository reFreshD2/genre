<form method="post" action="{{ path('possible_value_delete', {'id': possible_value.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить запись?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ possible_value.id) }}">
    <button class="btn">Удалить</button>
</form>
